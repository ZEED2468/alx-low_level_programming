#include "main.h"

/**
 * print_rev - prints to string in reverse
 * @str: Strin to print
 * Return: Nothing
 */

void print_rev(char *str)

{
	int len = strlen(s);

	while (len--)
		putchar(*(s + len));
	putchar(10);

}
